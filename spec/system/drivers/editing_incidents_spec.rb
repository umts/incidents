# frozen_string_literal: true

require 'spec_helper'

describe 'editing incidents as a driver' do
  it 'allows editing reports' do
  end
  context 'admin deletes the incident' do
    it 'displays a nice error message' do
    end
  end
end
